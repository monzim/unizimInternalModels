## Unizim Internal Models

This package contains the internal models used by the Unizim project for server and App.

```yaml
unizim_models:
  git:
    url: https://github.com/Monzim/unizimInternalModels.git
```
